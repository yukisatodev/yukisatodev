name: gitartwork from a contribution graph

on:
  push:
  schedule:
    - cron: '0 15 * * *'

jobs:
  build:
    name: Make gitartwork SVG
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: jasineri/gitartwork@v1
        with:
          user_name: yukisatodev
          text: YUKISATODEV
      - uses: jasineri/simple-push-action@v1
